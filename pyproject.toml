[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint]
# Keep the baseline lenient so existing prototype code passes; tighten later.
select = ["E4", "E7", "E9", "F", "I"]
